# API Documentation

## Server Endpoints

The HTTP server in `server.py` provides the following endpoints for Grafana integration:
- `http://localhost:8000/`: This endpoint sends a CSV file of the CAN logs.